import { colors } from "../../utils/colors";

export const styles = {
    badge: {
        '& .MuiBadge-badge': { bgcolor: colors.lightGreen }
    },
    icons: {
        color: colors.seaBlue
    }
}